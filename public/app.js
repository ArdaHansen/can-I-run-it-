let supabaseClient = null;
let currentSession = null;
let appState = { user:null, profile:null, runs:[], plan:null };
const $ = (s) => document.querySelector(s);
const app = $('#app');
function toast(msg){const t=$('#toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),4200)}
async function init(){
  try{const c=await fetch('/api/config').then(r=>r.json()); if(c.error) throw new Error(c.error); supabaseClient = supabase.createClient(c.supabaseUrl,c.supabaseAnonKey);}
  catch(e){app.innerHTML=`<section class="wrap"><div class="panel"><h2>Config fehlt</h2><p class="muted">${e.message}</p></div></section>`;return;}
  const { data } = await supabaseClient.auth.getSession(); currentSession=data.session; updateAuthButton();
  supabaseClient.auth.onAuthStateChange((_event, session)=>{currentSession=session;updateAuthButton(); if(location.hash.includes('dashboard')) loadDashboard();});
  window.addEventListener('hashchange', router); router();
}
function updateAuthButton(){const b=$('#authBtn'); if(!b)return; b.textContent=currentSession?'Logout':'Sign in'; b.onclick= async()=>{ if(currentSession){await supabaseClient.auth.signOut(); location.hash='/';} else location.hash='/auth';};}
function shell(title, inner){return `<section class="wrap"><div class="eyebrow">● V2.1 Stable</div><h1 style="font-size:clamp(38px,6vw,72px);letter-spacing:-.06em;margin:18px 0 22px">${title}</h1>${inner}</section>`}
function router(){const h=location.hash.replace('#','')||'/'; if(h.startsWith('/auth')) return authPage(); if(h.startsWith('/dashboard')) return dashboardPage(); if(h.startsWith('/profile')) return profilePage(); if(h.startsWith('/upload')) return uploadPage(); return homePage();}
function homePage(){app.innerHTML=`<section class="wrap hero"><div><div class="eyebrow">🔥 Race readiness for real runners</div><h1>Can I <span class="grad">Run It?</span></h1><p class="lead">Upload runs, build your profile and find out if your running goal is actually realistic. Not another generic plan app. A smarter running cockpit.</p><div class="actions"><a class="primary" href="#/upload">Upload a run</a><a class="secondary" href="#/dashboard">Open dashboard</a></div><div class="cards"><div class="card"><h3>Goal Check</h3><p>Sub 3, 10K PB or first half marathon. Get a clear readiness score.</p></div><div class="card"><h3>3-run rule</h3><p>Training suggestions unlock after 3 uploads so the plan has context.</p></div><div class="card"><h3>Streaks</h3><p>Build consistency without turning every run into a race.</p></div></div></div><div class="mock"><p class="muted">Current goal</p><h2 style="font-size:34px;margin:4px 0">Sub 3:30 Marathon</h2><div class="score grad">68%</div><p class="muted">Readiness estimate</p><div class="plan-day"><b>AI Coach</b><br><span class="muted">Base is improving. Add one longer aerobic run and keep easy days easy.</span></div><div class="statgrid" style="grid-template-columns:1fr 1fr;margin-top:14px"><div class="stat"><b>🔥 7</b><span class="muted">Streak</span></div><div class="stat"><b>3/3</b><span class="muted">Runs unlocked</span></div></div></div></section>`}
function authPage(){app.innerHTML=shell('Sign in',`<div class="dashboard"><div class="panel"><h2>Create account</h2><p class="muted">Use email auth. You may need to confirm your email once in Supabase. Later you can brand the email template.</p><form id="authForm" class="form"><label>Email<input name="email" type="email" required placeholder="you@example.com"></label><label>Password<input name="password" type="password" required minlength="6" placeholder="min. 6 characters"></label><div class="actions"><button class="primary" name="mode" value="signup">Create account</button><button class="secondary" name="mode" value="signin">Sign in</button></div></form></div><div class="panel"><h2>What happens next?</h2><p class="muted">After login you build your runner profile, upload 3 runs and unlock adaptive training suggestions.</p></div></div>`); $('#authForm').onsubmit=authSubmit;}
async function authSubmit(e){e.preventDefault(); const btn=e.submitter.value; const fd=new FormData(e.target); const email=fd.get('email'); const password=fd.get('password'); let res; if(btn==='signup') res=await supabaseClient.auth.signUp({email,password,options:{emailRedirectTo:location.origin+'/#/dashboard'}}); else res=await supabaseClient.auth.signInWithPassword({email,password}); if(res.error)return toast(res.error.message); toast(btn==='signup'?'Check your email to confirm.':'Signed in.'); location.hash='/dashboard';}
async function api(path, opts={}){if(!currentSession){location.hash='/auth';throw new Error('Not signed in')} const headers=opts.headers||{}; headers.Authorization='Bearer '+currentSession.access_token; const r=await fetch(path,{...opts,headers}); const j=await r.json().catch(()=>({})); if(!r.ok) throw new Error(j.error||'Request failed'); return j;}
async function refresh(){try{const d=await api('/api/me'); appState=d; return d;}catch(e){toast(e.message); return null;}}
async function dashboardPage(){app.innerHTML=shell('Dashboard',`<div class="panel"><p class="muted">Loading your cockpit...</p></div>`); await loadDashboard();}
async function loadDashboard(){const d=await refresh(); if(!d)return; const p=d.profile; const runs=d.runs||[]; const latest=runs[0]; const goal=p?`${p.goal_distance||'Marathon'} ${p.goal_time_hour||3}:${String(p.goal_time_min||30).padStart(2,'0')}`:'No profile yet'; const score=latest?.readiness_score||p?.readiness_score||0; app.innerHTML=shell('Dashboard',`<div class="dashboard"><div class="panel"><h2>Your running cockpit</h2>${!p?`<div class="lock"><b>Profile missing</b><p class="muted">Build your profile so Can I Run It can judge goals properly.</p><a class="primary" href="#/profile">Build profile</a></div>`:''}<div class="statgrid"><div class="stat"><b>${goal}</b><span class="muted">Goal</span></div><div class="stat"><b>${score}%</b><span class="muted">Readiness</span></div><div class="stat"><b>🔥 ${p?.streak||runs.length}</b><span class="muted">Streak</span></div><div class="stat"><b>${runs.length}/3</b><span class="muted">Runs uploaded</span></div></div><h2 style="margin-top:24px">Recent runs</h2><div class="list">${runs.length?runs.map(runCard).join(''):'<p class="muted">No runs yet. Upload your first one.</p>'}</div></div><div class="panel"><h2>AI Coach</h2>${latest?`<p>${latest.ai_analysis||'Upload more runs for better analysis.'}</p><div class="plan-day"><b>Next move</b><br><span class="muted">${latest.training_suggestion||'Keep training consistent.'}</span></div>`:'<p class="muted">Upload a run to get your first analysis.</p>'}<h2 style="margin-top:22px">Adaptive plan</h2>${renderPlan(d.plan,runs.length)}</div></div>`)}
function runCard(r){return `<div class="run"><div><b>${r.title||'Run'}</b><br><span class="muted">${r.distance_km||0} km · ${r.pace_text||'-'} · Effort ${r.effort||'-'}/10</span></div><b class="grad">${r.readiness_score||0}%</b></div>`}
function renderPlan(plan,count){if(count<3)return `<div class="lock"><b>Locked</b><p class="muted">Upload ${3-count} more run${3-count===1?'':'s'} to unlock training suggestions.</p></div>`; if(!plan)return '<p class="muted">Build your profile to generate a plan.</p>'; return `<p class="muted">${plan.summary}</p>${plan.plan.map(d=>`<div class="plan-day"><b>${d.day}: ${d.title}</b><br><span class="muted">${d.details}</span></div>`).join('')}`}
function profilePage(){app.innerHTML=shell('Runner profile',`<div class="panel"><form id="profileForm" class="form"><div class="row"><label>Name<input name="full_name" placeholder="Arda"></label><label>Username<input name="username" placeholder="ardasrunshit"></label></div><div class="row"><label>Level<select name="level"><option>Beginner</option><option selected>Intermediate</option><option>Advanced</option><option>Competitive</option></select></label><label>Experience<select name="experience"><option>&lt;1 year</option><option selected>1-3 years</option><option>3+ years</option></select></label></div><div class="row"><label>Goal distance<select name="goal_distance"><option>5K</option><option>10K</option><option>Half Marathon</option><option selected>Marathon</option></select></label><label>Target race<input name="target_race" placeholder="Hamburg Marathon"></label></div><div class="row"><label>Goal hours<input name="goal_time_hour" type="number" value="3" min="0" max="12"></label><label>Goal minutes<input name="goal_time_min" type="number" value="30" min="0" max="59"></label></div><div class="row"><label>Training days/week<input name="training_days" type="number" value="4" min="1" max="7"></label><label>Weekly km now<input name="weekly_km" type="number" value="30" min="0"></label></div><label>Available days<select name="available_days" multiple size="7"><option selected>Mon</option><option>Tue</option><option selected>Wed</option><option>Thu</option><option selected>Fri</option><option>Sat</option><option selected>Sun</option></select></label><label>Rest preference<select name="rest_preference"><option value="balanced">Balanced</option><option value="more_rest">More rest</option><option value="high_frequency">High frequency</option></select></label><label>Injuries / notes<textarea name="injury_history" placeholder="Knee pain, shin splints, no injuries..."></textarea></label><button class="primary">Save profile</button></form></div>`); $('#profileForm').onsubmit=saveProfile; prefillProfile();}
async function prefillProfile(){if(!currentSession)return; const d=await refresh(); const p=d?.profile;if(!p)return; const f=$('#profileForm'); ['full_name','username','level','experience','goal_distance','target_race','goal_time_hour','goal_time_min','training_days','weekly_km','rest_preference','injury_history'].forEach(k=>{if(f[k]&&p[k]!=null)f[k].value=p[k]});}
async function saveProfile(e){e.preventDefault(); const fd=new FormData(e.target); const obj=Object.fromEntries(fd.entries()); obj.available_days=Array.from(e.target.available_days.selectedOptions).map(o=>o.value); try{await api('/api/profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(obj)}); toast('Profile saved.'); location.hash='/dashboard';}catch(err){toast(err.message)}}
function uploadPage(){app.innerHTML=shell('Upload run',`<div class="dashboard"><div class="panel"><h2>Run upload</h2><p class="muted">OCR is beta-safe: upload a screenshot, then correct the numbers manually. That keeps the coach reliable.</p><form id="runForm" class="form"><div class="drop"><input name="screenshot" type="file" accept="image/png,image/jpeg,image/webp"><p class="muted">Garmin, Strava, Apple Fitness or Nike Run Club screenshot</p></div><div class="row"><label>Distance km<input name="distance_km" type="number" step="0.01" required placeholder="12.3"></label><label>Duration min<input name="duration_min" type="number" required placeholder="62"></label></div><div class="row"><label>Pace text<input name="pace_text" placeholder="5:02/km"></label><label>Avg HR<input name="avg_hr" type="number" placeholder="154"></label></div><div class="row"><label>Effort 1-10<input name="effort" type="number" min="1" max="10" value="5"></label><label>Run type<select name="run_type"><option>Easy</option><option>Long Run</option><option>Tempo</option><option>Intervals</option><option>Race</option></select></label></div><label>Notes<textarea name="notes" placeholder="How did it feel?"></textarea></label><button class="primary">Analyze run</button></form></div><div class="panel"><h2>What you get</h2><div class="plan-day"><b>Readiness score</b><br><span class="muted">The app checks whether the run supports your goal.</span></div><div class="plan-day"><b>Coach feedback</b><br><span class="muted">What to do next, based on intensity and distance.</span></div><div class="plan-day"><b>Plan unlock</b><br><span class="muted">After 3 uploads you unlock adaptive weekly suggestions.</span></div></div></div>`); $('#runForm').onsubmit=saveRun;}
async function saveRun(e){e.preventDefault(); const fd=new FormData(e.target); try{const j=await api('/api/runs',{method:'POST',body:fd}); toast(`Analyzed. Readiness: ${j.run.readiness_score}%`); location.hash='/dashboard';}catch(err){toast(err.message)}}
init();
